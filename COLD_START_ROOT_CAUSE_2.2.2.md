# Median Browser 2.2.2 冷启动根因与修复

## 结论

“冷启动后第一次打不开网页，刷新才正常”不是单一 URL 解析错误，而是首航在 WebView 尚未进入可见帧时提交、恢复回调不可靠、离线设置跨页面残留三条链路叠加。

## 已修复的根因

1. **Resume 不等于视图已挂载**
   - Android 可以先调用 `onResume()`，再把 DecorView 加入 WindowManager。
   - 旧逻辑在视图未 attach 时退化为普通 `Handler.post`，仍可能在首帧前调用 `loadUrl()`。
   - 现在先等待 attach，再 `postOnAnimation`，执行瞬间再次检查 Resume；暂停后会取消本次派发，恢复时重新派发。

2. **启动数据与 WebView 生命周期没有统一门禁**
   - 新 `StartupReadiness` 以无顺序位集等待 VIEW、DATA、RESUMED；私密模式额外等待 FILTERS 与 COOKIES。
   - 任意回调顺序、重复回调和“派发后立刻暂停”都有可执行测试，首次导航严格只执行一次。

3. **部分 WebView 只恢复历史，不启动主框架**
   - WebView 保存状态改放入独立嵌套 Bundle，避免 Activity 状态键干扰恢复。
   - 每个 WebView 独立监控第一次 HTTP(S) 导航；`onPageStarted` 即确认，未确认才在 1.5 秒后对同一目标补发一次。
   - 新目标会作废旧计时器；重定向视为成功；暂停期间不重试，Resume 后再检查。

4. **离线设置污染下一网页**
   - MHTML 会开启内容访问、阻断网络并关闭 JavaScript。
   - 旧站点设置缓存可能让回到同一域名时跳过 JavaScript/图片策略重设。
   - 现在离线转换会使缓存失效；进入网络页先恢复联网，再应用站点设置；主页恢复独立的脚本、图片和混合内容基线。

5. **恢复标签缺少目的页策略**
   - 冻结标签和系统状态恢复后，会按主页、HTTP(S)、MHTML、其他页面分别重建设置，不再沿用新 WebView 默认值或前页状态。

## 回归覆盖

- 六种主启动信号顺序。
- 派发与执行之间暂停，再 Resume。
- 普通/隐私窗口 exactly-once。
- 外部 Intent、地址栏排队输入、会话恢复、主页。
- 同 URL 去重、新 URL 作废旧重试、重定向确认、单次重试。
- 离线页 → 同站在线页、离线页 → 主页、冻结标签恢复。

容器内没有可用的 Android 真机或 KVM，因此这里完成的是生产代码共享的纯 Java 状态测试、完整 Release 构建和 APK 验证；仍建议在发生过问题的手机上重点复测一次真正的进程冷启动。
