# Median Browser 2.1.10 优化报告

## 边界

本轮严格保持功能集合、applicationId、数据结构、权限和 WebView 安全边界不变。没有新增浏览器功能，只优化设置流程、返回状态、主线程负载、绘制分配和启动图标。

## 优化大纲与完成状态

1. 建立设置、返回链与运行时热路径基线：完成。
2. 删除设置的重复中转层，统一返回状态：完成。
3. 移除主线程同步序列化和队列回退风险：完成。
4. 减少动画、绘制和跳转中的重复对象与等待：完成。
5. 用纯代码重做小尺寸图标并校验：完成。
6. R8 构建、静态门禁、签名和交付校验：完成。

## 关键变化

| 热点 | 原行为 | 2.1.10 |
|---|---|---|
| 设置入口 | 先选四个分类，再进入具体设置 | 一个分区清晰的根页，常用项少一次点击 |
| 设置返回 | 根设置返回主菜单，状态链容易继续触发 Activity 返回 | 根设置无父级，返回键只收起设置 |
| 编辑后回跳 | 固定等待 90 ms | 下一显示帧衔接，通常约 16 ms |
| 站点设置保存 | 每次点击都在调用线程遍历并生成最多 2000 站点的 JSON | 跳过无变化值，180 ms 合并，后台生成与写入 |
| 脚本/网络队列 | 队列满时 CallerRunsPolicy 可能在 UI 线程执行任务 | 固定工作线程 + worker-only 队列 |
| 网页进度 | 每次更新新建 ObjectAnimator，连续回调可能叠加 | 使用 API 26+ ProgressBar 单动画通道 |
| 设置图标绘制 | 每帧换算 dp，并计算 8 组 sin/cos | 构造时缓存密度，使用预计算方向向量 |
| 启动图标 | 字母 M、装饰元素较多 | 三段色彩的终端提示符，纯 VectorDrawable |

## 校验结果

- 53 个 Java 文件语法检查通过。
- Android XML 全量解析通过。
- NetworkSecurity、下载重试、地址识别、启动策略、自定义 CSS/HTML、主页配置、Logo 标记和生成脚本自测通过。
- R8 release 收缩通过。
- APK 包名 `com.xinyv.median.compat`、versionCode 74、versionName 2.1.10、minSdk 26、targetSdk 36 已核对。
- ZIP 完整性、4-byte zip alignment、v2/v3 签名及证书 SHA-256 复验通过。

## 签名事实

交付中的 PKCS12 与 1.4.1 以来的稳定证书 SHA-256 `80daf48c…e5e3da9` 完全一致。2.1.10 APK 使用同一证书和同一包名签名，versionCode 为 74，因此该稳定签名线上的较低版本可直接覆盖更新。后续版本必须继续使用这份 PKCS12。
