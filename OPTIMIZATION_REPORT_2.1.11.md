# Median Browser 2.1.11 极致优化报告

## 边界与结论

本轮没有增加浏览器功能，只优化原有功能的执行路径、资源策略、信息架构、返回行为和品牌图形。Via 官方把速度、极简、隐私和可定制作为核心定位；Median 2.1.11 针对这些维度逐项消除代码层面的明显短板。

“已经在所有设备上快于 Via”不能由代码审查直接证明。两者都依赖设备上的 System WebView，最终结论必须来自同一手机、同一内核、同一网络和同一页面集的 A/B 数据。本轮交付完成的是可审计的实现改造和可执行的验收门槛。

## 优化大纲与落地结果

| 维度 | 原先弱点 | 2.1.11 落地 | 同机验收目标 |
|---|---|---|---|
| 冷启动 | 首次 `new WebView` 和三份本地 JSON 都压在启动关键路径 | 先调用官方异步 WebView 启动边界；书签/会话、站点策略、用户脚本由 3 个后台任务并行解码 | 冷启动 P50 不慢于 Via，P95 不高于 Via 10% |
| 首帧 | WebView 初始化完成前没有可交互画面 | 纯原生轻量品牌面先绘制；WebView 回调完成后幂等替换，3 秒兜底防 OEM 丢回调 | 首帧无长时间纯白/纯黑，启动无 ANR |
| 隐私冷启动 | 独立进程同步创建 WebView，并在 UI 线程加载订阅索引 | WebView 提供方与过滤规则由有界双线程并行准备，Cookie 清理同步并发；全部就绪后才允许联网 | 隐私窗口冷启不白屏、不削弱过滤策略，关闭后无遗留工作线程 |
| 二级入口 | 下载记录和离线文件在第一次点开菜单时才解析/扫描 | 首屏稳定后由空闲启动线程预热两份本地索引；功能仍保持按需实例化 | 首次打开资料库与页面工具无可感知 I/O 停顿 |
| 导航调度 | 快速连续输入可能堆积已经过时的页面准备工作 | 单线程、容量 2 的“最新任务优先”队列；新导航主动清理旧准备任务 | 连续 20 次导航不出现旧任务反压 UI |
| 异步结果 | 页面分析、TLS、媒体和脚本回调可能在切页后继续更新当前界面 | 回调绑定发起时的 WebView 与导航序列；朗读另有代际取消令牌 | 快速切页/切标签后无旧弹窗、旧媒体结果或迟到朗读 |
| 通用后台任务 | 等待队列无明确上限，拥塞时存在内存增长风险 | 主任务、网络、密码库、订阅更新全部使用有界队列；拒绝时返回明确状态，不在 UI 线程执行 | 压测期间队列内存保持有界，主线程无回退执行 |
| 本地主页 | 相同配置可能重复生成 HTML、重复 `loadDataWithBaseURL` | 配置与前 12 个快捷书签组成稳定渲染键；HTML 和每个 WebView 的已渲染状态双层复用 | 反复回主页不重复装载相同文档 |
| 持久化 | 用户脚本每次改动同步构造完整 JSON 并写偏好 | 280 ms 合并、后台快照序列化和写盘；退出前 flush；无变化不写 | 连续开关脚本不阻塞点击或动画 |
| 内存 | 热标签与后台任务策略存在互相放大的可能 | 沿用设备分级热 WebView/冷状态上限，并让全部新线程池支持 30 秒空闲回收 | 5/20 标签 PSS 与回收时延不劣于 Via |
| 耗电 | “极限性能”曾在普通网页持有 Wi-Fi 高性能/低延迟锁 | 删除浏览 Wi-Fi 锁和持续唤醒；性能档只调整缓存、渲染优先级、标签预算和任务时机 | 30 分钟标准模式耗电不高于 Via 10% |
| 主菜单 | 18 个平铺动作，页面、资料、隐私和设置混在一起 | 收敛到 12 个一级动作，按浏览、当前页面、资料工具、浏览器分区；隐私工具二级聚合 | 常用动作不超过两次点击，一级菜单一眼可扫完 |
| 浏览器设置 | 分类与动作层级不稳定，开关和二级页混排 | 根设置 9 项；HTTPS、跟踪、Cookie 原地切换，其余按打开、搜索、主页、性能、存储、关于分组 | 返回键只退一层，不跳桌面 |
| 网站设置 | 15 项平铺，兼容、权限、显示混杂 | 收敛为兼容模式、内容与隐私、权限、显示、重置、清数据 6 个入口 | 单站策略可在明确分组内找到 |
| 返回体验 | 系统返回、面板返回和退出浏览器曾存在分叉 | Android 13+ 预测性返回与传统返回统一进入同一状态机：二级页 → 设置根 → 网页历史 → 标签 → 退出 | 设置中的返回操作 100% 不直接回桌面 |
| 安全策略 | 为追求启动速度可能过早恢复外部网页 | 只有站点策略和 document-start 用户脚本就绪后才恢复网络页；旧 WebView 不支持可靠能力时继续关闭高权限脚本 | 性能优化不改变原有安全默认值 |

## WebView 异步启动策略

- 支持 `ASYNC_WEBVIEW_STARTUP_V2` 时走当前稳定的 Consumer 边界。
- 只支持旧 `ASYNC_WEBVIEW_STARTUP` 时走 InvocationHandler 边界。
- 两者都不支持、接口调用失败或发生链接异常时立即回到普通同步构造。
- OEM 接受请求但不回调时，3 秒后走同一个幂等构造函数；迟到回调不会创建第二个 WebView。
- 独立隐私进程复用同一异步边界和 3 秒兜底，但必须先设置独立数据目录；过滤规则、WebView 和清理状态通过三方栅栏汇合，不能用“先联网后补规则”换速度。
- 精简兼容层保留了反射协议名和 R8 keep 规则，没有引入完整 AndroidX WebKit 运行时依赖。

## Median 图标识别系统

图标不再从字母 M、浏览器罗盘或终端提示符出发，而是把 Median 这个词的数学含义直接变成符号：

- 水平的五个位置代表一个奇数规模的有序集合。
- 中心空心环代表中位元素，竖直切面强调“从中心切分”。
- 四个白点只负责建立顺序和节奏；唯一的琥珀小点附着在中心，不抢走黑白主体。
- 默认主页的 `median` 小写字标把同一琥珀色放在中心字母 `i`，让启动图标和产品名形成记忆闭环。
- 标准、自适应、圆形和 Android 13 单色图标全部由 VectorDrawable 描述，不包含位图资产。

这套形状与 M、Termux 的命令提示符、Via 的圆形航向符号都没有结构依赖，缩到通知栏或启动器小尺寸仍能读出“中心值”。

## 已完成的自动门禁

- Java 语法与全部 Android XML 解析。
- 网络边界、下载重试、地址栏分类、主页打开策略、自定义 CSS/HTML、主页配置、Logo 标记纯 Java 回归测试。
- 用户脚本生成 JavaScript 的 Node 语法检查。
- Java 17 编译、AAPT2 资源链接、R8 release 压缩、zipalign 与 APK v2/v3 签名验证。
- 静态门禁覆盖普通/隐私异步 WebView、隐私启动栅栏、有界队列、本地索引预热、过期页面回调、预测性返回、主页渲染缓存、脚本合并写盘、无浏览 Wi-Fi 锁和原创图标资源。

## 尚不能伪装成已完成的部分

需要真实设备才能给出 Via A/B 结论：冷启动 P50/P95、页面首屏、5/20 标签 PSS、连续浏览耗电、掉帧、ANR 和 Top 100 网站兼容率。源码中的 `COMPETITIVE_READINESS.md` 给出了同机、同内核、同网络的判定规则；在数据产生前，发布文案不应写“全面快于 Via”。

参考：

- Via 官方定位：https://viayoo.com/zh-cn/
- Android 官方 WebView 启动优化：https://developer.android.com/develop/ui/views/layout/webapps/optimize-webview-startup
