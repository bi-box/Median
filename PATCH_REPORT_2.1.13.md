# Median Browser 2.1.13 修复报告

## 全站无感深色

- Activity 在创建任何 WebView 前应用明暗主题信号，WebView 初始化时即配置背景和原生算法深色。
- document-start 脚本覆盖所有来源，在页面首帧前设置深色根背景；不支持该能力的内核仍在 `onPageStarted` 立即执行同一兜底。
- 删除按网站加载完成后执行的整页反色方案，不再使用 `filter: invert()` 或 `hue-rotate()`。
- 兜底只转换浅色背景、深色文字和浅色边框；图片、视频、画布、SVG、对象和嵌入内容不改色。
- DOM 以每帧 220 个节点增量处理，MutationObserver 跟进动态内容；没有处理到固定节点数就停止的限制。
- 全局切换会遍历当前及后台 WebView，并原地重绘浏览器栏和已打开的设置面板。

## 收藏与文件夹

- 收藏编辑器支持直接创建、名称/网址修改、文件夹选择和删除，不要求先打开网站。
- 文件夹树支持多层子文件夹、重命名、递归删除和主页显示开关；删除文件夹时网站安全移回根目录。
- 主页 Median Logo 支持长按入口；设置和书签资料页提供等价入口。
- 标记为主页显示的文件夹使用纯 CSS 文件夹图形，与普通 favicon 快捷网站混排。
- 独立 SharedPreferences 只保存轻量文件夹元数据和网址归属，不复制书签正文；普通与完整备份均包含该结构。

## 体积与验证

- 未添加生产依赖、SDK、位图或后台常驻服务。
- 深色 JavaScript 生成结果通过 Node 语法检查，并断言无整页反色、无站点节点总量上限。
- 文件夹自测覆盖嵌套路径、移动、循环拒绝、导出恢复、主页标记和递归删除。
- 全量 Java/API 36 编译、R8、APK ZIP、v2/v3 签名及 500 KiB 预算由发行构建再次验证。
