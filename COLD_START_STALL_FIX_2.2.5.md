# Median Browser 2.2.5 冷启动 10% 停滞修复报告

## 2.2.4 为什么仍会卡住

2.2.4 已避免从 `shouldOverrideUrlLoading()` 内直接嵌套网络请求，但仍有两个判断不成立：

1. 部分 System WebView 在只接受导航、尚未真正推进主框架时，就会触发 `onPageStarted()` 并把进度设为约 10%。2.2.4 此时提前确认成功，450 毫秒恢复任务随即失效。
2. 主页仍先发起一次 `median://` 页面导航。即使 Java 侧延后提交 HTTP(S)，厂商实现仍可能保留内部协议的取消状态，随后吞掉真实网址。

截图中“主页仍可见、进度固定在首段、手动刷新后立即恢复”与这一状态完全一致。

## 2.2.5 修复

- 内置主页通过 WebChromeClient 的 prompt 回调发送命令，不再让 WebView 导航到 `median://`。
- 命令携带进程启动时生成的 128 位随机令牌；只接受当前活动 WebView、已验证的内置主页和 `median://` 命令，普通网页与自定义主页无法调用。
- 保留内部协议解析作为兼容入口，但正常主页搜索、网址输入、搜索引擎切换、书签与文件夹入口均走无导航副作用的命令通道。
- `onPageStarted()` 只更新页面状态，不再确认首航成功；只有 `onProgressChanged()` 越过 10% 才撤销恢复资格。
- 450 毫秒仍停在初始阶段时：当前 URL 已是目标则调用原生 `reload()`，否则重新 `loadUrl()`；不再执行可能互相竞争的 `stopLoading()` + `loadUrl()`。
- 隐私窗口同样不再用 `onPageStarted()` 提前确认，10% 停滞仍保留一次恢复机会。

## 纯代码与体积

- 首页脚本复用 DOM 查询、事件默认行为和 URL 编码操作，减少脚本解析工作。
- 随机令牌直接从受信任的主页 meta 读取，避免在生成脚本时复制令牌和新增 DEX 字符串索引。
- 没有删除功能、资源、权限或兼容 API；资源目录保持不变。
- 构建继续使用原有 raw-DEFLATE/Stored 方式，没有使用 Zopfli 或替代压缩器。

| 指标 | 2.2.4 | 2.2.5 |
|---|---:|---:|
| APK | 207,317 B | **207,317 B** |
| R8 `classes.dex` | 419,784 B | **419,748 B** |
| raw-DEFLATE DEX | 199,110 B | **199,132 B** |
| versionCode | 89 | **90** |

## 验证

| 检查 | 结果 |
|---|---|
| Java / Android XML 静态检查 | 通过 |
| 生成主页与 UserScript JavaScript 语法 | 通过 |
| 首航、启动门禁、内部导航自测 | 通过 |
| 媒体、网络、安全、下载与主页自测 | 通过 |
| APK v2 签名 / 证书 | 通过 / 与 2.2.4 相同 |
| ZIP 完整性 / 对齐 | 通过 |
| 包名 / 版本 | `com.xinyv.median.compat` / `2.2.5 (90)` |
| APK SHA-256 | `275e6c5134faf171589cb7e742b22778b68068c3aff46675ac308cba56330fde` |

当前构建环境没有连接 Android 真机或模拟器，因此不能声称已完成设备侧复现验证。安装后应彻底结束进程，连续三次执行“冷启动 → 立即从主页搜索或打开快捷入口”，确认进度不再停留于 10%。
