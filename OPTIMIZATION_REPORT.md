# Median Browser 2.1.17 极限优化报告

## 结果

- 基线：2.1.16，230,271 bytes
- 发布包：2.1.17，199,125 bytes
- 减少：31,146 bytes（13.53%）
- 包名：`com.xinyv.median.compat`
- 版本：`versionCode 81` / `versionName 2.1.17`
- 签名：APK Signature Scheme v2
- 更新证书 SHA-256：`80daf48c091d6174981c2a176360b42ac32463d23ddc9e5f3c95c0951e5e3da9`

2.1.17 与 2.1.16 的包名及证书完全一致，版本号递增，可覆盖安装并保留应用数据。

## 体积优化

- 精简 R8 保留规则，允许访问级别收缩、跨类合并及激进重命名。
- APK 仅保留 6 个必要 ZIP 条目；复用圆形/单色启动图资源，删除重复图标和无效备份规则资源。
- 26 个运行时自绘图标改为紧凑路径表：路径只解码一次，并由全部按钮共享。
- 用户脚本合并为每个 WebView 一次 document-start 注册，移除逐脚本同步测速与重复回调对象。
- 删除未启用的 WebView 异步启动协议、预热 WebView、下载内存诊断及无效遥测字段。
- 下载断点状态由对象列表压成单游标，同时兼容读取旧版单段状态。
- 首页静态 CSS/JS 合并为常量模板，减少 DEX 指令和运行时 `StringBuilder.append` 次数。
- DEX 使用 R8 后再以 Zopfli 进行确定性 raw-deflate 重压缩。
- 签名后仅删除 APK Signing Block 中不参与签名的可选 Verity 对齐填充；随后由官方 `apksigner` 重新验证。

## 速度与内存优化

- 深色模式仅走 System WebView 原生 algorithmic darkening，不扫描 DOM、不注入全页深色脚本、不强制刷新。
- 4 个小型本地状态读取合并为一个后台顺序任务，减少线程争用、原子计数器和 UI 回调层级。
- 去除不会启用的 WebView 预热实例，降低启动内存峰值。
- 脚本 document-start 从 N 次边界注册降为 1 次；页面结束回退也不再回传逐脚本 JSON 性能数据。
- 下载固定使用 128 KiB 缓冲区和最多 2 个任务线程，避免运行时诊断、模式规划和探测请求。
- 图标常态绘制由大型分支降为一次共享 `drawPath`，没有位图解码和第三方图标库。

## 验证

- Java 语法及 Android XML 解析通过。
- 主页、Logo、自定义 CSS/HTML、书签文件夹、搜索框输入、内部导航、用户脚本安装、下载策略/重试/文件类型自测通过。
- 生成的主页 JavaScript 与用户脚本兼容层通过 Node 语法检查。
- APK ZIP 完整性、AAPT badging、zipalign 和 v2 签名验证通过。
- 新旧 APK 的包名和签名证书指纹已逐项对比。

## 继续缩小的边界

在保留当前浏览器功能、纯 Java/XML、同一更新证书以及 v2 签名的前提下，199,125 bytes 已低于严格的 200,000-byte 门槛。剩余体积主要是 WebView 边界协议、书签/文件夹、用户脚本兼容层、下载中心、隐私窗口和主页自定义等真实功能。继续大幅压缩将需要删除功能、放弃旧数据迁移或降低签名/兼容性，不属于无损优化。
