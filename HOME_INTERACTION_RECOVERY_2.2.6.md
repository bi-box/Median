# Median Browser 2.2.6 主页交互恢复报告

## 问题与根因

2.2.5 为避开 `median://` 导航副作用，把内置主页命令改为随机令牌认证的 `prompt()` 通道，但 Java 在 `onJsPrompt()` 尚未返回时同步执行了搜索或内部页面切换。

部分 System WebView 不允许在 WebChromeClient prompt 回调内重入导航。结果是命令已经到达 Java，却被提供方阻塞或丢弃，表现为主页搜索框、刷新按钮、快捷收藏和收藏文件夹全部没有反应。刷新还受主页渲染键缓存影响：内容未变时 `showHome()` 会直接返回，更像按钮完全失效。

## 2.2.6 修复

- `onJsPrompt()` 只负责验证随机令牌、活动 WebView、实时主页 URL、受信内置主页和 `median://` 命令格式。
- Java 先调用 `result.confirm("")` 结束 prompt，再用 `view.post()` 把命令放到下一条 UI 消息中；不再从 WebChromeClient 回调内重入导航。
- 异步任务执行前再次验证 Activity、活动标签、当前 URL、内置主页信任状态和自定义主页隔离，防止页面在排队期间切换后误执行旧命令。
- 主页身份依据 `view.getUrl()` 的实时状态判断，不依赖厂商 WebView 传入的回调 URL。
- 主页刷新先删除当前 WebView 的渲染键缓存，再重新生成并载入主页。
- 普通窗口与独立隐私窗口使用同一套“确认后异步执行”规则。
- 首页脚本使用 ES5 兼容语法，不依赖箭头函数、`let` 或 `NodeList.forEach`，兼容旧版及厂商 WebView。

## 验证

| 检查 | 结果 |
|---|---|
| Java 语法与 Android XML | 通过 |
| 生成主页 / UserScript JavaScript 语法 | 通过 |
| 生成主页行为：搜索、引擎、收藏、文件夹 | 通过 |
| 冷启动、首航、内部导航与主页策略自测 | 通过 |
| 媒体、脚本、网络安全与下载自测 | 通过 |
| APK v2 签名、ZIP 完整性与对齐 | 通过 |
| 包名 / 版本 | `com.xinyv.median.compat` / `2.2.6 (91)` |
| APK 大小 | `211,413` 字节 |
| APK SHA-256 | `ce785a45b26615e836839a2c2234dae794c9fa7aff90bc4303f9324e43951d7a` |

## 设备验证边界

当前隔离构建环境无法启动 Android 模拟器，也没有连接真机，因此不能把自动测试等同于设备侧“绝对无 bug”证明。安装后应先彻底结束进程，再依次验证主页中央搜索、快捷收藏、收藏文件夹、右上刷新和顶部地址栏；如厂商 System WebView 仍有差异，需要提供系统 WebView 版本与一次操作录屏继续定点修复。
