# Median Browser 2.1.16 修复报告

## 问题与处理

1. 文件夹删除入口隐藏且缺少可靠反馈
   - 把删除提升到当前文件夹页面。
   - 删除存储 API 返回明确成功状态，并加入递归文件夹、收藏回根目录测试。

2. 脚本安装步骤多且容易超时
   - 输入入口同时接受 URL 和源码，并预填有效剪贴板内容。
   - 扩展网页安装链接识别及长按操作。
   - 对 DNS、连接、读取和可重试 HTTP 状态执行一次自动重试。
   - `@require` 与 `@resource` 复用既有有界网络线程池并发读取，不新增长驻线程。

## 体积与安全

- 未引入第三方生产依赖。
- 脚本原生下载仍只接受 HTTPS，并在每次重定向后重新验证公网目标。
- 证书错误和永久性 HTTP 错误不会盲目重试。
